<UserControl x:Class="CSharpPractice4.Views.PersonListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSharpPractice4.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Surname" Binding="{Binding Surame}"></DataGridTextColumn>
                    <DataGridTextColumn Header ="Email" Binding ="{Binding Email}"></DataGridTextColumn>
                    <DataGridTextColumn Header ="Birthday date" Binding ="{Binding Birthday}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Birthday" Binding="{Binding IsBirthdayToday}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Adult" Binding="{Binding IsAdult}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Western Sign" Binding="{Binding WesternSign}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Chinese Sign" Binding="{Binding ChineseSign}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
    </Grid>
</UserControl>
